MONGODB_URI=
MONGODB_DB=
NEXT_PUBLIC_BASE_URL=
# Runware API key used by the room visualizer (server-side only).
# Create one at https://my.runware.ai/keys
RUNWARE_API_KEY=
# Optional override; defaults to FLUX.2 [klein] 9B Base (AIR: runware:400@3).
# RUNWARE_VISUALIZER_MODEL=runware:400@3
